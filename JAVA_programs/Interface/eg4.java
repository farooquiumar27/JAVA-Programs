// we can implement the interface

interface aaaa
{
public void sam( );
}
class bbb implements aaaa
{

};

/*
But this code will not compile because if we implements the interface then we  have  to define the
the method which is declared inside the interface
We will see that in eg5.java and eg6.java
*/
