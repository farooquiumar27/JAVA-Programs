// we can implement the interface

interface aaaa
{
public void sam( );
}
abstract class bbb implements aaaa
{

};

/*
But this code will compile because if we implements the interface and we do not define the
the method which is declared inside the interface
The we have to declare the as abstract
*/
